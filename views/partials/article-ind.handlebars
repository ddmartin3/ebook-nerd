<div class="article-ind">
        <h2>{{headline}}</h2>
         <div>
            <h4>Preview:</h4>
            <a href="{{url}}">
                <p>
                    {{summary}}
                </p>
            </a>
        </div>
        <button class="save" data-id="{{_id}}">Save Article</button>
</div>
